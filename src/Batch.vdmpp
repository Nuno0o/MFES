class Batch
types
-- TODO Define types here
values
	public maxWeight = 5.0;
instance variables
	private type : Product`Type;
	private weight : real;
	inv weight >= 0 and weight <= maxWeight;
	
operations
	public Batch: Product`Type ==> Batch
	Batch(Type) == (type := Type; weight := 0;return self);
	
	public addWeight: real ==> ()
	addWeight(Weight) == (weight := weight + Weight)
	pre Weight > 0;
	
	public removeWeight: real ==> ()
	removeWeight(Weight) == (weight := weight - Weight)
	pre Weight <= weight;
	
functions
-- TODO Define functiones here
traces
-- TODO Define Combinatorial Test Traces here
end Batch